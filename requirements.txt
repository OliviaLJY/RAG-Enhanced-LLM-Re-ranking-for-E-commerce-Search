# Core retrieval / dataset
datasets==2.19.0
rank_bm25==0.2.2
numpy==1.26.4
tqdm==4.66.4

# LLM reranking + attribute decomposition
openai==1.30.1

# Evidence retrieval (Day 6 v0). Pulls in torch + transformers (~2 GB).
sentence-transformers==2.7.0
